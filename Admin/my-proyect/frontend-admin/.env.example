# Variables de entorno de frontend-admin/ (Vite: prefijo obligatorio VITE_).
# Ref: tasks.md T005 — URL base de la API sin hardcodear endpoints.
# Ref: plan.md → Technical Context (frontend-admin consume la API REST del backend Java/Spring Boot).

# URL base de la API REST del backend (contracts/openapi.yaml). No incluir barra final.
VITE_API_BASE_URL=http://localhost:8080/api/admin
