/**
 * Enum RolUsuario.
 *
 * Ref: data-model.md → Enums / Value Objects → RolUsuario
 * Ref: tasks.md T006, spec.md FR-002, FR-007, US1, US3
 *
 * Determina qué acciones administrativas están disponibles para el usuario autenticado.
 */
export enum RolUsuario {
  USER = "USER",
  ADMIN = "ADMIN",
}
